# DMS lab modules

Code here is for the **DMS track** (Business Central, accounting, GL).

- Prefer additive modules and tables.
- CRM sales/credit improvements still go on `main` first; weekly merge feeds this branch.
- BC credentials: use sandbox only until promotion (see `bc-config.ts`).
